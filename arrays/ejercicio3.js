/**
 * 3.- Indica la función que comprueba si un objeto es o no un Array. Demuestra su uso mediante un ejemplo.
 * 
 * La función que demuestra si un objeto es un array o no es isArray().
 * 
 * @author Pablo
 */

{
    let coches = ["Mercedes", "Volvo", "Renault", "Ford", "Seat"];
    let cadenita = "Hola";

    console.log(Array.isArray(coches));
    console.log(Array.isArray(cadenita));
}
